<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}Tłumacz przysięgły (DE){% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('assets/styles/app.css') }}">
    <link rel="stylesheet" href="{{ asset('assets/styles/theme-clean.css') }}">
    <style>
        html, body { height:100%; margin:0; }
        body { display:flex; flex-direction:column; font-family: "Segoe UI", Roboto, Arial, sans-serif; }
        main { flex:1; }
        h1, h2, h3 { font-weight: 600; }
        p { line-height: 1.6; }
        .container { max-width: 1100px; }
        .navbar-brand { font-weight: bold; }
        .navbar-nav .nav-link { padding: 0.5rem 0.9rem; }
        footer { font-size: 0.9rem; }
    </style>
</head>
<body>
<header class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand fw-bold" href="{{ path('home') }}">Tłumacz przysięgły (DE)</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#topnav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <nav id="topnav" class="collapse navbar-collapse">
            {% set r = app.request.attributes.get('_route') %}
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <li class="nav-item"><a class="nav-link {% if r=='home' %}active{% endif %}" href="{{ path('home') }}">O mnie</a></li>
                <li class="nav-item"><a class="nav-link {% if r=='services' %}active{% endif %}" href="{{ path('services') }}">Usługi</a></li>
                <li class="nav-item"><a class="nav-link {% if r=='conditions' %}active{% endif %}" href="{{ path('conditions') }}">Warunki</a></li>
                <li class="nav-item"><a class="nav-link {% if r=='pricing' %}active{% endif %}" href="{{ path('pricing') }}">Cennik</a></li>
                <li class="nav-item"><a class="nav-link {% if r=='clients' %}active{% endif %}" href="{{ path('clients') }}">Klienci</a></li>
                <li class="nav-item ms-lg-2">
                    <a class="btn btn-outline-light {% if r=='contact' %}active{% endif %}" href="{{ path('contact') }}">Kontakt</a>
                </li>
            </ul>
        </nav>
    </div>
</header>

<main>
    <div class="container py-5">
        {% block content %}{% block body %}{% endblock %}{% endblock %}
    </div>
</main>

<footer class="border-top bg-light">
    <div class="container d-flex justify-content-between align-items-center py-3 small text-muted">
        <div>© {{ "now"|date("Y") }} Tłumacz przysięgły języka niemieckiego</div>
        <div>NIP / REGON / adres – uzupełnij</div>
    </div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
{% block javascripts %}{% endblock %}
{% block importmap %}{{ importmap('app') }}{% endblock %}
</body>
</html>
